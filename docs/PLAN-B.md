---
title: "План разработчика B — оболочка приложения"
subtitle: "Prostor · 24–29 августа 2026"
---

# Зона ответственности

`src/app/**`, `src/features/projects`, `inspector`, `history`, `persistence`, `export`,
`src/shared/ui`. Внутрь `features/canvas` не заходишь. Контракт правится только вместе с A,
коммит с префиксом `contract:`.

**Твой скилл-workflow:** `feature-slice` — процедура заведения нового слайса в `features/`.
Напишешь во вторник, дальше пользуешься им четыре раза.

Дополнительно на тебе: инфраструктура репозитория, хуки, CI и оформление сдачи.

---

# Понедельник 24 августа, вечер (3–4 часа)

Первая половина — совместная с A (контракт), вторая — твоя.

| # | Что | Готово когда |
|---|---|---|
| 1 | Совместно: `shared/types/document.ts` и срез стора | в `main` |
| 2 | Скелет: Vite + React 19 + TS, Tailwind, Biome, Vitest, Playwright | `npm run dev` поднимается |
| 3 | Скрипты `dev`, `build`, `test`, `e2e`, `typecheck`, `lint` в `package.json` | все запускаются |
| 4 | `.claude/settings.json` и хуки из `docs/TOOLING.md` | `brew install jq`, хуки скопированы, `DEV_NAME` выставлен у обоих |
| 5 | `/init` → `CLAUDE.md`, дописать руками по разделу 5 `TOOLING.md` | файл в `main` |
| 6 | GitHub Actions: typecheck, lint, test, build | зелёный прогон |
| 7 | Ветка `feat/app-shell` | создана |

**Проверь хук журнала сразу.** Отправь любой промпт и посмотри, появился ли
`sessions/<твой ник>/2026-08-24.md`. Если нет — чинить сейчас, потому что задним числом
журнал не восстановить.

**Коммиты:** `chore: скелет проекта`, `chore: хуки и настройки Claude Code`, `chore: CI`

---

# Вторник 25 августа — проекты и хранение

**Цель дня:** можно создать проект, он переживает перезагрузку страницы.

| Блок | Что делаем |
|---|---|
| Утро | Три субагента: (1) маршруты и layout, (2) список проектов и пустое состояние, (3) слой IndexedDB через `idb` |
| День | Автосохранение с дебаунсом 500 мс, индикатор состояния, сохранение вьюпорта |
| День | Хранилище blob для картинок — API, которым будет пользоваться A. Согласуй с ним сигнатуру утром, ему это нужно в среду |
| Вечер | Скилл `feature-slice` через `/skill-creator` |

**Определение готовности:**

- чистый браузер показывает пустое состояние, а не пустую сетку;
- создание проекта уводит сразу на холст;
- перезагрузка на холсте открывает тот же проект;
- переименование, дублирование, удаление с подтверждением работают;
- индикатор показывает «Сохранение…» и «Все изменения сохранены».

---

# Среда 26 августа — инспектор и клавиши

**Цель дня:** выделенный объект можно настроить, руки не отрываются от клавиатуры.

| Блок | Что делаем |
|---|---|
| Утро | Три субагента: (1) панель свойств по типам узлов, (2) множественное выделение — общие свойства, (3) палитра и поповеры на `radix-ui` + `react-colorful` |
| День | Горячие клавиши на `tinykeys` по таблице 6.3 ТЗ |
| Вечер | Экран справки по `?`, тосты через `sonner` |

**Определение готовности:**

- панель меняет содержимое при смене типа выделения;
- при множественном выделении показаны общие свойства и изменение применяется ко всем;
- при пустом выделении панель показывает свойства холста;
- цвет применяется мгновенно, без кнопки «Применить»;
- все сочетания из таблицы 6.3 срабатывают, `Esc` выходит из режима ввода.

---

# Четверг 27 августа — история и экспорт

| Блок | Что делаем |
|---|---|
| Утро | `zundo`: undo/redo, глубина 50 |
| День | Склейка шагов: перетаскивание — один шаг, ввод текста — один шаг на сессию редактирования. Это главная сложность дня |
| День | Экспорт PNG (вся доска и выделенное, 1× и 2×) и JSON с картинками в base64 |
| Вечер | Импорт JSON с валидацией через `zod`; **первый merge в `main`** |

**Настройка `zundo`:** `partialize` исключает `viewport`, выделение и активный инструмент —
иначе панорамирование забьёт всю историю. `handleSet` с дебаунсом 400 мс склеивает
подряд идущие изменения одного узла.

**Определение готовности:** критерии приёмки 7, 9, 10 из раздела 10 ТЗ проходят.

---

# Пятница 28 августа — перекрёстная проверка и вид

| Блок | Что делаем |
|---|---|
| Утро | Merge по `workflows/pre-merge.md`, разбор конфликтов с A |
| День | **Агент-ломатель по зоне A**, отдельная сессия. Ломаешь движок напарника |
| День | 3–4 сценария Playwright: создать проект, собрать диаграмму, отменить, экспортировать |
| Вечер | `/frontend-design` по панелям: типографика, ритм, контраст, тёмная тема |

**Что пробовать ломателем на движке A:** линия с обоими концами на одной фигуре, удаление
фигуры с пятью привязанными линиями, зум на 400 % и перетаскивание за край экрана,
картинка 20 МБ, картинка нулевой ширины, текст на 10 000 символов, объект с нулевым
размером, поворот на 720°, тысяча узлов и `Cmd+A`, вставка из буфера картинки во время
редактирования текста.

**Определение готовности:** все находки в `REPORT.md` по опасности; `/security-review`
прогнан один раз.

---

# Суббота 29 августа до 18:00 — сдача

| # | Что | Как |
|---|---|---|
| 1 | Прогон всех 12 критериев приёмки | вдвоём, вслух, по списку |
| 2 | `README.md` | запуск по шагам, скриншот, **кто что делал** — обязательное требование задания |
| 3 | `REPORT.md` | по всем пунктам, включая раздел «что не сработало» |
| 4 | ТЗ в PDF через `/pdf` | плюс к оформлению |
| 5 | Проверка на секреты | `.env`, `settings.local.json`, история команд, скриншоты |
| 6 | Бонусы: изоляция, инъекция | см. `docs/PROMPTS-SHARED.md` |
| 7 | Отправка ссылки в телеграм `@oleg56x` | приватный репозиторий — доступ `@gfarhad93` |

**Не откладывай `REPORT.md` на субботу.** Пиши по одному абзацу каждый вечер, пока помнишь,
что именно не получилось. В субботу вспоминается только хорошее, а оценивают как раз
честные тупики.

## Если стартуете во вторник

Понедельничный блок (контракт, скелет, обвязка) переезжает на утро вторника,
всё остальное сдвигается на день. Первым срезается день на вид и полировку
(пятница, вечерний блок), вторым — ломаная и Безье в коннекторах.
Обязательная часть при этом остаётся целой.
