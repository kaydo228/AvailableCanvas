# Журнал решений

Сюда идёт то, что через месяц вызовет вопрос «почему так странно сделано»:
отход от ТЗ, компромисс ради производительности, обход ограничения библиотеки.
Очевидное не пишем.

Формат: дата · решение · отвергнутая альтернатива · почему.

---

## 2026-08-25 · `ConnectorNode` не наследует `BaseNode`

Так в разделе 5 ТЗ. У линии нет прямоугольной рамки, поэтому `x`, `y`, `width`,
`height`, `rotation` ей не принадлежат.

**Следствие, с которым живём:** `node.x` по типу `Node` не проходит без сужения
по `type`. Добавлены `BoxNode`, `isConnector()`, `isBoxNode()` — без них
не собирается ничего, что трогает геометрию.

**Альтернатива:** дать коннектору фиктивную рамку по описанному прямоугольнику.
Отвергнута: рамка была бы производной величиной, и любой её сдвиг трансформером
означал бы бессмысленную операцию над линией.

## 2026-08-25 · `canvasSize` в сторе

Добавлены `canvasSize` и `setCanvasSize`. Пишет `CanvasStage` через `ResizeObserver`.

**Зачем:** `zoomToFit` и `zoomToSelection` без размера канваса не посчитать,
а тянуть размер из DOM внутри стора — значит привязать стор к рендеру.

**Статус:** правка общего контракта, требует подтверждения зоны B.

## 2026-08-25 · Компонент сетки называется `GridLayer.tsx`, а не `Grid.tsx`

Файлы `grid.ts` и `Grid.tsx` различаются только регистром. На macOS они уживаются
в файловой системе, но TypeScript отказывается собирать проект
(`differs only in casing`).

**Вывод на будущее:** при нарезке задач между параллельными агентами смотреть
на имена файлов, а не только на границы ответственности.

## 2026-08-25 · Сетка рисуется одним `Shape`, а не тысячами `Circle`

Все точки набиваются в один путь и закрашиваются одной операцией. Точка — квадрат
2×2 px: на таком размере от круга неотличим, а `ctx.arc` заметно дороже.

Слой сетки намеренно не масштабируется вьюпортом — координаты считаются сразу
экранные, иначе на четырёхкратном приближении вместо точек были бы кляксы.

**Предохранитель:** не более 20 000 точек за кадр; при превышении шаг удваивается,
а если и это не помогает — сетка не рисуется.

## 2026-08-25 · Узлы холста: фигуры, текст, стикеры

### Оверлей ввода один на все типы

`TextOverlay` общий для подписи фигуры, текстового узла и стикера.
Три отдельных разъезжаются по поведению (где-то Escape отменяет, где-то нет),
и пользователь это чувствует.

**Найденный при проверке руками баг:** `display: flex` на самой `<textarea>`
не центрирует её содержимое — текст всё равно прижимался к верху, и при входе
в редактирование подпись фигуры прыгала. Исправлено обёрткой: центрирует
внешний блок, а поле внутри растёт по содержимому через `scrollHeight`.

### Подпись выравнивается по `label.align`, а не жёстким центром

`TextOverlay` берёт `textAlign` из стиля. При двух источниках выравнивания
текст прыгал бы в момент входа и выхода из редактирования. Вертикальное
центрирование при этом жёсткое — его требует FR-03.

### Рамка выделения по габаритам, с `strokeScaleEnabled={false}`

Обводка по контуру треугольника и ромба читается хуже. Немасштабируемая
толщина нужна, чтобы на 400 % рамка не превращалась в жирную полосу,
а на 10 % не исчезала.

### `event.cancelBubble = true` на клике по узлу

Иначе обработчик на `Stage` примет тот же клик за клик по пустому месту
и сбросит выделение, которое узел только что поставил. Накладывает на
`CanvasStage` обязательство не рассчитывать на всплытие клика от узлов.

### Необязательные поля передаются условным спредом

Из-за `exactOptionalPropertyTypes` передача `undefined` в необязательный проп —
ошибка компиляции. Отсюда `node.dash ? { dash: node.dash } : {}` и
`Object.assign(base, overrides)` вместо спреда.

### Стикер всегда квадратный, `Shift` на него не влияет

Прямоугольный стикер выглядит как ошибка. Параметр `shiftKey` в сигнатуре
оставлен ради единообразия с остальными инструментами.

### Кегль стикера подбирается, а не задаётся

`fitFontSize` идёт вниз с шагом 1 от заданного кегля до минимума 8.
Бинарный поиск экономил бы три вызова измерения ценой того, что при
немонотонном переносе слов он может промахнуться.

Функция принимает **измерение параметром**: в jsdom настоящего измерения
текста нет, и подгонять тест под нулевые габариты — значит проверять пустоту.
Если контекста canvas нет, подбор вырождается в «взять исходный кегль»:
лучше не ужать, чем ужать по выдуманным цифрам.

### Текст и стикер открываются на ввод сразу после создания

Класть пустой стикер и потом целиться в него двойным кликом — лишний жест.
Фигура так не делает: у неё подпись необязательна.

### Пустой текстовый блок удаляется при потере фокуса

Иначе один промах мимо холста инструментом «Текст» оставляет невидимый узел,
который переживает перезагрузку и попадает в экспорт. Пробелы считаются
пустотой: набор пробелов выглядит ровно так же, как ничего.

## 2026-08-25 · Картинки и трансформер

### Проверки формата и размера не дублируются

`putImage` из зоны B — граница доверия: файл приходит от пользователя через
диалог, drop или буфер. Свой список форматов на стороне холста разъехался бы
с тем, что реально принимает хранилище.

### Ровно один потребитель слушает Cmd+V

`useImageInsert` зовут и панель (ради кнопки), и холст (ради drop). Пока
слушатель `paste` вешали оба, **одна вставка создавала две картинки** — поймано
при проверке руками. Теперь подписка включается параметром, и говорит `true`
только холст.

### Трансформер появился раньше плана

Без него нельзя выполнить требование «ресайз пропорциональный, Shift
освобождает». Полное выделение рамкой и множественное — отдельно.

**Размер берётся из модели, а не из `shape.width()`.** Узлы обёрнуты в `<Group>`,
а у группы Konva собственной ширины нет — она всегда 0, и произведение
на масштаб схлопывало узел в минимальные 8×8. Поймано вживую: картинка
исчезала при первом же движении ручки.

Масштаб после фиксации сбрасывается в 1: размер уехал в модель, и второе
применение растило бы узел на каждое касание ручки.

### Правило пропорций видно сразу при выделении

У картинки показываются только угловые ручки, у фигуры — все восемь. Набор
ручек и есть подсказка о действующем правиле; если пересчитывать его только
в момент перетаскивания, пользователь узнаёт правило уже после того, как
испортил объект.

### Первый замер холста синхронный

`ResizeObserver` срабатывает через кадр, и вставка сразу после открытия
проекта видела нулевой размер — картинка ложилась в начало мировых координат
вместо центра экрана. Воспроизводилось только на первой вставке после
загрузки, поэтому и не попалось сразу.

### Картинка меньше 600 px не растягивается

Вписывание только уменьшает. Растянутая маленькая картинка выглядит хуже
исходной, а вернуть исходный размер пользователь уже не сможет.

## 2026-08-25 · Форматирование проскакивало мимо хука

`format-file.sh` висит на `PostToolUse` для `Write|Edit` и видит только записи,
прошедшие через эти инструменты. Запись через heredoc, `sed` или скрипт его
обходит — форматирование копилось молча и вылезло сразу двумя десятками
замечаний на `Stop`.

**Что сделали:** добавлен `format-changed.sh` на `PostToolUse` для `Bash`.
Список файлов берётся у git (`git status --porcelain`), а не у инструмента, —
так хук не зависит от того, каким способом файл записали.

**Отвергнуто:** форматировать весь проект после каждой команды. Дёшево по времени
(111 файлов за 40 мс), но переписывало бы и чужие файлы, подтянутые из ветки
напарника, — в диффе появлялся бы шум, к работе не относящийся.

**`gate-stop.sh` теперь сначала чинит, потом проверяет.** Безопасные автоправки
применяются молча, человека дёргают только там, где нужно решение. `--unsafe`
не включён: он меняет смысл кода.

### Отдельно: bash 3.2

Первая версия `format-changed.sh` использовала `mapfile` — это bash 4, а в macOS
системный bash 3.2. Хук падал **молча**, то есть в точности воспроизводил ту
проблему, которую был призван закрыть. Поймано только потому, что хук прогнали
руками сразу после написания.

Вывод на будущее: каждый новый хук проверять явным прогоном на `/bin/bash`,
а не считать рабочим по факту написания.

## 2026-08-25 · Имя разработчика больше не берётся из окружения

Журнал сессий дважды за день уходил в `sessions/unknown/`. Второй раз — уже
после того, как `DEV_NAME` был выставлен в `~/.zshenv` и проверен в терминале:
`zsh -c 'echo $DEV_NAME'` отвечал правильно, а хук всё равно писал в `unknown`.

**Причина:** процесс Claude Code забирает окружение при старте. Хуки — его
дочерние процессы и наследуют ровно то окружение; правка профиля до них
не доходит до перезапуска. Проверка в терминале при этом врёт: `Bash`-инструмент
поднимает новую оболочку, которая профиль перечитывает.

**Решение:** хук резолвит имя цепочкой — `DEV_NAME` → файл `.claude/dev-name`
→ `git config user.name` → `unknown`. Файл и git-конфиг читаются в момент
вызова, поэтому действуют сразу и переживают перезапуск.

**Отвергнуто:** `env` в `.claude/settings.json`. Файл общий и коммитится —
имя одного участника попало бы второму. `settings.local.json` не коммитится,
но его правка запрещена собственным хуком `guard-secrets.sh`.

**Урок общего свойства:** механизм, который при неудаче молчит и подставляет
значение по умолчанию, скрывает собственную поломку. Оба раза проблему заметил
человек, а не система.

## 2026-08-25 · Выделение и трансформации

### Выделение на отдельном слое Konva

Рамка и трансформер живут во втором `<Layer>`, не в общем с узлами. В общем
слое каждый клик и каждое движение рамки перерисовывали бы всю доску —
на тысяче узлов это видно сразу.

Konva при этом не возражает против трансформера, привязанного к узлам чужого
слоя: ручки, поворот и групповое перетаскивание проверены вживую.

### Рамка выделяет по пересечению, а не по накрытию

Требовать полного накрытия — значит заставлять обводить большую картинку
целиком. Все редакторы ведут себя так же.

Заблокированные узлы рамкой не берутся: замок в инспекторе должен защищать
и от случайного захвата, иначе он бесполезен.

**Найдено тестом:** рамка нулевой площади считалась пересечением, и любой
клик по узлу «задевал» бы его, сбрасывая набор до одного элемента. Вырожденная
рамка теперь не берёт никого — это клик, а выделением по клику занимается
отдельная ветка.

### Порядок слоёв: направление обхода имеет значение

При множественном выделении сдвиг вверх идёт с верхнего конца, вниз — с нижнего.
Если начать не с того конца, узлы группы перепрыгивают друг через друга
и их взаимный порядок перемешивается.

Узел, упёршийся в потолок, не даёт подняться следующему за ним выделенному —
иначе группа «схлопывается» к краю вместо того, чтобы остановиться целиком.

### Групповое перетаскивание перехватывается на слое

Konva тянет только тот узел, за который взялись. Дельта раздаётся остальным
выделенным через всплывающие события `dragstart`/`dragmove`/`dragend`
на обёртке слоя — рендереры про выделение не знают и знать не должны.

### Шаг стрелок — в мировых единицах

Не в экранных: иначе одно и то же нажатие двигало бы объект на разное
расстояние по доске в зависимости от зума.

## 2026-08-26 · Соединители, этапы 1 и 2

### Инвариант 3 обеспечивается конструкцией, а не проверкой

Наружу из `connectors/geometry.ts` торчат только `pointEndpoint` и
`nodeEndpoint`. Собрать через них конец с обоими полями разом или без
единого нельзя. Проверять постфактум ненадёжно: достаточно одного места,
где объект собрали руками, и инвариант тихо сломан.

`reattachEndpoint` в сторе по той же причине заменяет конец **целиком**,
а не правит по полям: дописать `nodeId` к концу, у которого уже есть
`point`, — самый простой способ получить оба поля.

### `auto` сравнивает доли от полуразмеров, а не абсолютные смещения

У широкой низкой фигуры почти любая цель оказывается «сбоку» по абсолютной
величине, и линия лезла бы из торца вместо верха. Есть тест ровно на этот
случай.

### Оба конца в режиме `auto` разрешаются через ориентиры

Сначала для каждого конца берётся ориентир — центр фигуры или сама точка, —
и только потом сторона выбирается относительно ориентира противоположного
конца. Иначе получается рекурсия: чтобы выбрать сторону слева, надо знать
точку справа, и наоборот.

### Радиус прилипания к якорю — в экранных пикселях

В мировых единицах на мелком зуме якорь стал бы недосягаем, на крупном
прилипал бы через пол-экрана. Целиться человек должен одинаково на любом зуме.

Отпустил внутри фигуры, но мимо якорей — `auto`: показали фигуру, а не
сторону, и решать, откуда выходить линии, должна программа.

### Наконечники рисуются отдельно от линии

`Konva.Arrow` умеет только одинаковые наконечники на обоих концах, а ТЗ
требует независимые. Поэтому тело линии — `Line`, наконечники — отдельные
`Line` и `Circle`.

Под линией лежит невидимая полоса шириной 12 px: попасть мышью в линию
толщиной 2 px невозможно.

### Найдено вживую: узлы перетаскивались при активном инструменте «Линия»

Протяжка, начатая на фигуре, тянула саму фигуру — Konva видит нажатие
на `draggable`-узле раньше, чем событие доходит до инструмента. Линия при
этом рисовалась, но фигура уезжала, и выглядело это как полная каша.

Узлы теперь слушают мышь только под «Выбором». Побочная польза: инструментом
создания больше нельзя случайно выделить объект вместо создания нового.

## 2026-08-26 · Соединители, этапы 3 и 4

### Порядок в удалении принципиален

`removeNode` считает координаты якорей **до** удаления узла, пока он ещё
в документе. После удаления считать не от чего, и конец пришлось бы ставить
в ноль — линия прыгнула бы в начало координат.

Проверено вживую: координаты свободного конца до и после удаления совпадают
до пикселя.

### Падения при обращении к удалённому узлу не случилось

Ожидалось, но не воспроизвелось. Причина в том, что путь разрешения концов
изначально сделан «мягким»: `connectorEnds` возвращает `null`, если узел
не найден, а рендерер на `null` просто ничего не рисует. Инвариант 4 обязан
такого не допускать, но рендер не должен падать из-за его нарушения —
это два независимых рубежа, и здесь сработал второй.

Отдельный тест на сценарий «три фигуры, пять линий, удаляю среднюю» —
14 проверок в `connectors/detach.test.ts`.

### Живой пересчёт стоил трёх изменений в рендере

Раньше линия догоняла фигуру только на отпускании: стор обновлялся
в `dragend`. Обновление на каждом кадре само по себе даёт дёрганье другого
рода — перерисовку всей доски, — поэтому понадобилось:

1. ~~**Узкая подписка у линии.**~~ **Эта запись была ложной.** Правка
   в `ConnectorView` не применилась: скрипт печатал «ok» независимо от того,
   совпала ли строка замены, а типы сходились, потому что файл оставался
   валидным. Обнаружено ревьюером в ночном прогоне 26 августа, тогда же
   и сделано по-настоящему — см. запись «Прокладка соединителей»
   от 26 августа в конце файла.
2. **`memo` на рендерерах узлов** — иначе перерисовываются все узлы,
   а не только сдвинутый.
3. **Стабильные колбэки в `NodesLayer`.** Без них `memo` бесполезен:
   новая функция на каждый рендер слоя считается изменившимся пропом.

Первое без второго и третьего не работает — это одно изменение, разнесённое
по трём файлам.

### Что НЕ измерено

Перетаскивание теперь пишет в стор каждый кадр. На трёх узлах это незаметно,
но NFR-01 (тысяча объектов, не ниже 50 fps) на этом пути **не замерялся**.
Замер — отдельным пунктом, до него утверждать, что производительность
в порядке, нельзя.

## 2026-08-26 · Прокладка соединителей: ломаная, кривая, подпись

Полный разбор — в `docs/nightly/connectors/`, здесь только решения.

### Маршрут отдаётся плоским массивом чисел, а не объектом

Не ради красоты. `shallow` из zustand сравнивает объекты на **один уровень**
через `Object.is`, и объект с полем-массивом давал бы новую ссылку на каждый
вызов — то есть цикл перерендеров, а не «мемоизация не сработала». Массивы
он сравнивает поэлементно.

Это же исправляет **ложную запись** от 25 августа: узкая подписка
в `ConnectorView` была записана как сделанная, но правка не применилась.
Теперь сделана по-настоящему.

### «Навстречу друг другу» считается по якорям, а не по концам вылетов

Вылет ужимается до половины зазора. При зазоре меньше двух вылетов концы
совпадают, разность обращается в ноль, а `Math.sign(0)` не равен `±1` —
и ветка «навстречу» не срабатывала никогда. На доске это выглядело как шип
наружу на 40 единиц посреди 20-единичного промежутка между фигурами.

### Ломаная обходит только две связанные фигуры

Третьи не обходит. Это `A*`-роутинг, он вне ТЗ. Ограничение осознанное,
записано, чтобы через месяц не выглядело недоделкой.

### Пустая подпись — это пустое значение, а не отсутствие ключа

`exactOptionalPropertyTypes` и решение зоны B «панель никогда не удаляет
ключ» другого пути не оставляют. На доске следов не остаётся: не рисуется
ни текст, ни подложка.

### Один случай — один откат

Починка чужого `anchor` внесла новый дефект: `routing.ts` откатывался
на автоподбор стороны, `geometry.ts` — на центр рамки. Линия рисовалась
из якоря, а при удалении фигуры конец прыгал в центр. Два разных отката
на один случай — это всегда расхождение, найденное или ещё нет.

### Правки файлов скриптом — только с проверкой факта замены

`str.replace` молча ничего не делает при несовпадении строки. Дважды
из-за этого потерялась одна и та же правка, причём первый раз она попала
ложью в коммит и в этот журнал. Скрипт обязан падать, если строка
не найдена.

## 2026-08-26 · История (FR-10) — zundo, а не свой стек команд

`temporal` из zundo обёрнут вокруг `immer` в `shared/store/board.ts`, настройки
лежат в `features/history/model/temporal.ts`.

**Альтернатива:** свой стек команд с `do`/`undo` на каждое действие. Отвергнута:
действий в контракте уже под тридцать, обратная операция понадобилась бы каждому,
и каждая новая пара действие-обратное — это место, где они разъедутся. Снимки
поверх immer стоят дёшево — неизменившиеся ветки документа переиспользуются.

### Вьюпорт заменяется заглушкой, а не выкидывается из снимка

`viewport` лежит **внутри** `document`, а undo делает поверхностный merge по
верхнему уровню. Выкинь ключ — документ приедет вообще без вида. Поэтому
в снимке на его месте общая замороженная заглушка, а живой вид возвращает
`undo` из `features/history/model/store.ts`.

**Следствие:** `useBoardStore.temporal.getState().undo()` напрямую звать нельзя,
камера уедет в начало координат. Наружу торчит только `undo` из слайса.

Заодно это даёт главное свойство: панорамирование и зум в историю не попадают
вовсе — в снимке вьюпорта нет, и по остальным ключам документ совпадает. Иначе
`Cmd+Z` отматывал бы камеру вместо действий, а полсотни шагов истории съедались
бы одним движением трекпада.

### Дебаунс запоминает ПЕРВОЕ состояние серии

`handleSet` копит 400 мс и пишет шаг от состояния, с которого серия началась,
а не от предпоследнего кадра. Перетаскивание — это десятки `moveNodes`; откат
должен вернуть узел туда, где его взяли.

**Альтернатива:** throttle из примеров zundo. Отвергнут: он пишет шаг каждые
400 мс, и секундное перетаскивание превращается в три шага истории.

### Открытие проекта чистится явно, а не угадывается

`CanvasScreen` зовёт `clearHistory()` сразу после `loadDocument`. Догадываться
по состоянию, «это загрузка или правка», не выходит: загрузка документа того же
проекта (StrictMode монтирует эффект дважды) от обычной правки по снимку
неотличима, а цена ошибки — первый Cmd+Z опустошает доску.

### Ввод текста держит шаг открытым до blur

Пока `editingNodeId !== null`, таймер шаг не закрывает; закрывает его
`holdHistory(false)` из подписки в `useHistorySession`. Проверка стоит в момент
срабатывания таймера, а не постановки, поэтому «поставил стикер и напечатал
в нём» — один шаг, а не два: пауза в наборе истории не разрывает.

## 2026-08-26 · Экспорт и импорт (FR-12)

### Область PNG считается по модели, а не по тому, что видно

`lib/bounds.ts` обходит узлы документа и складывает габариты, повёрнутые узлы —
по четырём углам. Вьюпорт в расчёт не входит вообще.

**Альтернатива:** снять то, что сейчас на экране (`stage.toDataURL()` без
аргументов). Отвергнута: экспорт зависел бы от того, куда пользователь отвёл
камеру, и на доске шире экрана всегда резал бы половину. Konva перерисовывает
сцену в новый холст, поэтому за краем экрана для него ничего особенного нет.

`pixelRatio = множитель / zoom` — из-за этого 1× означает один пиксель на
единицу доски при любом текущем зуме, а не «как сейчас на экране».

### Сетка в PNG не попадает, фон подкладывается руками

Сетка рисуется в экранных координатах на видимую область (так задумано в
`GridLayer`), и за её пределами её просто нет — в картинке вышла бы полоса
точек на четверть доски. Поэтому слой сетки на время съёмки прячется.

Фон подкладывается своим `fillRect` перед `drawImage`: Konva отдаёт прозрачный
PNG, а доска с прозрачным фоном в тёмном просмотрщике — чёрное на чёрном.
Отсюда `toCanvas` вместо голого `toDataURL`: в `toDataURL` фон не передать.

### Перед съёмкой снимается выделение

Рамки выделения рисует зона A внутри того же слоя, и без этого они запекаются
в картинку — особенно заметно в режиме «только выделенное», где обведён каждый
объект. Стор меняется на два кадра и возвращается обратно; шагом истории это
не становится (снимок истории выделение не хранит).

### Импорт всегда заводит НОВЫЙ проект

**Альтернатива:** спрашивать «заменить текущую доску?». Отвергнута: импорт,
который может стереть открытую доску, требует подтверждения, отмены и
объяснений, а новый проект — это ноль риска и ноль вопросов.

Картинки перекладываются под новыми `blobId`: id из чужого файла может совпасть
с занятым, и тогда две доски начнут делить одну картинку — удаление на одной
сломает вторую.

### Проверка файла — zod, и версия проверяется до схемы

Файл приходит с чужой машины: это граница доверия, а не «там же наш формат».
Схема в `lib/fileFormat.ts` повторяет модель и заодно проверяет инвариант 1
(order ↔ nodes) и наличие всех картинок внутри файла.

Версия сверяется ДО схемы: иначе на файле из будущей версии пользователь
получил бы «ожидалась единица, получена двойка» вместо «файл сделан более новой
версией, обновите приложение».

Лишние поля при своей версии не ошибка — zod их отбрасывает, доска открывается.
Ругаться на неизвестное поле значит ломать совместимость с любым будущим
экспортом, который добавит одно поле.

## 2026-08-26 · Цвет оболочки живёт в токенах, тёмная тема — их переопределение

Компоненты не знают ни одного hex: только смысловые имена — `bg-sheet`,
`text-pencil`, `border-rule`, `bg-accent`. Сами значения лежат в трёх блоках
`src/index.css`: светлая как база, системная тёмная (`prefers-color-scheme`,
но только пока пользователь не выбрал светлую руками) и явный `data-theme`.

**Альтернатива:** `dark:`-варианты Tailwind у каждого класса. Отвергнута: это
и есть «выборочная замена цветов в двадцати местах», только записанная в
разметке. Любой новый компонент забыл бы половину `dark:`, а поправить оттенок
серого стало бы нельзя без обхода всех файлов.

`@theme inline` обязателен. Без `inline` Tailwind подставляет в утилиты
значение переменной, посчитанное на `:root` в светлой теме, и переключение
перестаёт работать.

Затемнение под диалогом — свой токен `--scrim`, а не `bg-ink/35`: в тёмной
теме чернила светлые, и прозрачные чернила осветлили бы экран вместо затемнения.

## 2026-08-26 · Тема ставится до первой отрисовки

Скрипт в `index.html` читает localStorage и вешает `data-theme` на `<html>`
раньше загрузки бандла. Иначе тёмная страница моргает белым на каждой
перезагрузке. По умолчанию атрибута нет — решает система.

## 2026-08-26 · Моноширинный — для того, что измерено

Golos Text для всего, что пишет человек (имена досок, заголовки, подписи),
IBM Plex Mono для всего, что намерила машина: даты, координаты, проценты зума,
число объектов, hex цвета, клавиши. Это не украшение, а разделение двух видов
содержимого: в панели свойств сразу видно, где значение, а где подпись к нему.

Кириллица тут не мелочь: интерфейс русский, и обе гарнитуры рисовались с ней,
а не дополнялись ею.

## 2026-08-26 · Пустая доска показывается точками, а не иконкой

Карточка проекта без превью и пустое состояние рисуют ту же точечную сетку,
что и холст (`@utility grid-dots`). Иконка-заглушка сообщает «картинки нет»,
сетка — «здесь пустая доска»: второе честнее и мгновенно узнаётся.

## 2026-08-27 · Порченый документ чиним, а не отвергаем

Документ приходит двумя путями: из файла при импорте и из IndexedDB при
открытии проекта. Соблазн — на обоих отвечать отказом, раз уж завели проверку.
Так делать нельзя.

Файл чужой, отказ по нему честен и уже так работает. База своя, и в ней у людей
**уже лежит порча**, записанная до появления проверки: `zoom: 0`, дубли
в `order`, `Endpoint` без единого поля. Отказ на этом входе означает «твоя
доска больше не открывается» — то есть проверка отняла бы у человека работу,
которую сама же и не уберегла.

Поэтому правило одно на оба входа: **чинить всё, что чинится однозначно,
и отказывать только там, где починка была бы гаданием.** Однозначное — зажать
`zoom` в `[0.1, 4]`, выбросить дубль из `order`, поднять нулевой размер
до единицы, зажать `opacity`. Гадание — ничего из этого; список закрыт
и лежит в `features/persistence/repair.ts`.

Отдельно про ключ в `nodes`, не совпадающий с `node.id`. Он выглядит как
гадание — что правда, ключ или поле? — но им не является: на ключ ссылаются
`order` и концы линий, а `node.id` как адрес не используется нигде. Значит,
правда ключ, и это починка, а не отказ.

Починка на импорте показывается тостом со списком. Молча править чужой файл
нельзя: человек отдавал одно, а открыл другое, и узнать об этом он должен
сразу, а не по кривой доске через неделю.

## 2026-08-27 · Замок запрещает перемещение, но не оформление

`locked` соблюдал только холст: `draggable` и рамка выделения. Оболочка про
него не знала вовсе — Delete сносил заблокированный узел, стрелки его двигали,
инспектор правил координаты.

Замок теперь запрещает удаление, сдвиг стрелками и правку геометрии в панели.
Цвет, прозрачность и сам переключатель замка остаются доступными. Причина
простая: если выключить и их, замок нечем снять. И по существу — «нельзя
трогать вообще ничего» это режим только для чтения, другая функция с другим
названием; замок же значит «не сдвинь случайно».

Проверка стоит в местах вызова, в зоне B (`bindings.ts`, секции инспектора),
а не в действиях стора: те принадлежат зоне A.

## 2026-08-27 · «Разные» значения не применяются одним кликом

FR-09 запрещает кнопку «Применить» у цвета: пикер зовёт `onChange` на каждое
движение указателя, цвет узла едет за курсором, и это правильно — пока цвет
у выделенного ОДИН.

При разных значениях правило обратное. Вести за курсором нечего (пикер
стартует с выдуманного `#ffffff`), зато есть что потерять: поповер открывается
прямо под курсором, и один случайный клик затирал цвета всех выделенных узлов
значением, не связанным ни с одним из них. Поэтому при `value === undefined`
правка копится в черновике и уходит по явному нажатию «Применить ко всем».
Однородное выделение работает как работало — условие ровно одно.

То же у ползунка прозрачности, только там он ещё и врал: подпись «—»,
а бегунок стоял на `min`, будто у всех ноль. Теперь при разных значениях
ползунка нет вовсе, вместо него кнопка «задать всем N%»: свести значения
в одно надо явно, и только потом тянуть.

## 2026-08-27 · Две вкладки: честный отказ вместо слияния

Вкладки писали документ вслепую, побеждала последняя, правки первой исчезали,
и обе показывали «Все изменения сохранены».

Слияние досок не делаем. Два человека в одной вкладке — не наш случай
(бэкенда нет), а автоматически сводить две версии одной доски без истории
операций значит выдумывать результат, которого не хотел никто.

Вместо этого вкладка помнит `updatedAt`, с которым открыла проект, и отдаёт его
каждой записи. `saveDocument` сверяет его в той же транзакции и при расхождении
не пишет: индикатор переходит в `conflict`, тост зовёт обновить страницу.
`BroadcastChannel` доносит чужую запись сразу, а не при следующей попытке
сохранить — иначе вкладка узнаёт, что устарела, когда её правки уже потеряны.

Канал — уведомление, а не источник правды: пропущенное сообщение ничего
не ломает, потому что запись всё равно сверяется с базой.

## 2026-08-27 · Картинки чистятся по документам, а не по узлам

`deleteBlob` не вызывался нигде, кроме собственного теста, и картинки текли
навсегда. Очевидная починка — сносить блоб, когда удалён последний
ссылающийся узел, — неверна: узел вернётся по Cmd+Z, а картинка уже стёрта,
и отмена превратится в дыру.

Поэтому `collectOrphanBlobs` считает по **документам**: блоб живой, пока
на него ссылается хоть один документ в базе. Удаление узла картинку не трогает
вовсе. Удаление проекта — трогает, но через общий пересчёт, а не по списку
из удалённого документа: дублирование проекта делает две доски с одним
`blobId`, и снос «по факту был в этом документе» пробил бы дыру в копии.

`sweepBlobs()` зовётся ещё и при старте — подмести то, что натекло раньше.

## 2026-08-27 · Группа — контейнер, а не рисуемый объект

У группы нет ни заливки, ни обводки в модели, и рисовать поверх содержимого ей
нечего. `GroupView` рисует пунктирную рамку и только когда группа выделена:
без неё непонятно, почему клик по одной фигуре подсветил ещё четыре.

Отвергнут вариант «прямоугольник во всю рамку с хит-тестом». Он выглядит проще —
клик по группе ловится сам собой, перетаскивание работает штатным механизмом, —
но группа с широко расставленным содержимым превращается в невидимую заслонку:
перехватывает клики по пустоте между участниками и по чужим узлам, оказавшимся
под ней. Поэтому `listening={false}`, а клик по участнику разворачивается
в выделение всей группы на уровне слоя (`selection/groupSelection.ts`).

У рамки намеренно нет `id`. По id узлы ищут в дереве Konva трансформер
и перетаскивание набора; найдя рамку, они взялись бы за неё как за обычный узел
и записали бы в модель координаты со сдвигом на отступ. Рамка производная —
её пересчитывает `resyncGroups` по содержимому.

## 2026-08-27 · Рамка группы хранится полем, а не считается каждый раз

`GroupNode` наследует `BaseNode`, то есть у него есть `x/y/width/height` —
так в разделе 5 ТЗ, и трогать это не стали. Значит, рамка дублирует то, что
и так выводится из содержимого, и её надо держать в актуальном состоянии.

Альтернатива — считать рамку на лету и игнорировать поля — отвергнута: их читают
инспектор, экспорт и `zoomToFit`, и каждому пришлось бы знать про группы
отдельно. Вместо этого `resyncGroups` пересчитывает рамку на каждом изменении
геометрии участника, поднимаясь вверх по `groupId` до самой внешней группы.

Плата — обход на каждом кадре перетаскивания. Он линейный по числу участников
и на фоне отрисовки незаметен; NFR-01 замерен, деградации нет.

## 2026-08-27 · Группа собирается из выделения, а не инструментом

Процедура `canvas-node-type` требует для нового типа узла инструмент,
создающий объект протяжкой и кликом. Для группы шаг пропущен намеренно:
группу нельзя нарисовать, её собирают из уже существующих объектов по Cmd+G.
Инструмента в панели нет, тип `Tool` не расширялся.

Остальные шаги процедуры выполнены: тип уже был в модели, рендерер
зарегистрирован в карте, клавиши на месте, узел попадает и в `nodes`,
и в `order`, запрос напарнику заведён, тесты на инварианты зелёные.

## 2026-08-27 · Что группа делает с содержимым при удалении и роспуске

Удаление группы уносит содержимое. На экране это один объект, и оставить после
него россыпь узлов значило бы соврать про то, что человек удалил.

Роспуск, наоборот, содержимое сохраняет и переводит его в выделение — так видно,
что именно лежало в группе. Вложенная группа при роспуске отдаёт содержимое
внешней, а не наружу: иначе узлы выпадали бы из группы, которую никто
не распускал.

Коннекторы в группу не берутся — инвариант 2. Причина не формальная: у линии
нет рамки, двигать у неё нечего, и поехав за группой концами, она отвязалась бы
от фигур, к которым привязана.

## 2026-08-27 · Шестиугольник вписан в рамку, а не правилен

Шестая форма из FR-03 — flat-top: горизонтальные стороны сверху и снизу,
острые вершины по бокам. Скосы отрезают по четверти ширины с каждого края.

Фигура вписана в рамку `width × height` целиком и тянется вместе с ней, как
эллипс и ромб. Равносторонним она выглядит только при отношении высоты
к ширине √3/2 ≈ 0.866, в остальных случаях это её пропорциональное сжатие.

Отвергнут вариант «подгонять габариты под правильный шестиугольник»: рамка
узла общая для всех форм, её задаёт пользователь протяжкой, и фигура обязана
её заполнять. Иначе выделение, трансформер и рамка группы считали бы одно,
а на экране было бы другое.

Шаг процедуры `canvas-node-type` про модель пропущен: шестиугольник —
не новый тип узла, а шестое значение `ShapeNode['shape']`. Правка `document.ts`
всё равно затрагивает общий контракт, поэтому коммит с префиксом `contract:`.

## 2026-08-27 · Правильные многоугольники — одним генератором

Семиугольник добавлялся так же, как шестой формой добавлялся шестиугольник,
и стало видно, что второй раз выписывать вершины руками не надо: у правильной
фигуры они считаются на окружности. `regularPolygon` в `ShapeView` считает
вершины на единичной окружности и растягивает их bounding box на рамку узла.

Растягивает, а не вписывает в эллипс. У семиугольника нет вершины ровно
на «три часа», и при простом вписывании фигура не доставала бы до правого
края: между ней и рамкой выделения оставалась бы щель, у каждого n своя.
Заполнение рамки — то же правило, что уже выбрано для шестиугольника.

Треугольник и ромб остались заданы явно: правильными они не бывают, их вершины
привязаны к серединам и углам рамки, а не к окружности.

Шестиугольник переехал на общий генератор и не сдвинулся ни на пиксель —
его четырнадцать тестов проходят без правок, плюс добавлена явная сверка
координат. У нечётного числа сторон нет ни одной из симметрий, на которые
опирались тесты шестиугольника, и подогнать генератор под семиугольник,
не заметив шестого, было бы легко.

Клавиша — цифра «7», а не буква: свободных мнемоничных букв не осталось
(`С` занята стикером), а одиночные цифры ничем не заняты — под вид отведены
только сочетания с `$mod`. Иконки семиугольника в lucide нет, взят
восьмиугольник: ближе всего по силуэту, а подпись называет форму словом.

## 2026-08-27 · Дублирование: смещение и судьба половины линии

Копия смещается на шаг сетки (24 мировых единицы). Ноль — копия невидима
под оригиналом и её начинают дублировать повторно; произвольные «10 пикселей»
сбивают выравнивание по сетке, ради которого объект туда и ставили.

Линия, у которой скопирован только один конец, отвязывается вторым концом
в точку: берутся координаты, где конец был на момент дублирования, плюс то же
смещение. Отвергнут вариант «оставить конец на оригинальной фигуре» — копия
легла бы этим концом ровно на исходную линию, и перетаскивание оригинальной
фигуры двигало бы копию. То же правило уже действует при удалении узла
(инвариант 4): отвязка с сохранением координат, а не прыжок в ноль.

Заблокированные узлы дублируются, и замок уезжает в копию. Замок защищает
объект от правки, а не от чтения; при этом дублирование не становится обходом
замка — снять его можно инспектором, осознанно.

---

## 27.08 · PDF собирается своим конвейером, без новых зависимостей

**Решение.** `scripts/build-pdf.mjs`: свой разбор нужного подмножества markdown
плюс печать через Chromium, который уже стоит вместе с Playwright.
Запуск — `npm run docs:pdf`.

**Отвергнуто:**

- **pandoc** — внешняя программа, которую надо ставить на машину каждому,
  кто соберёт документ. Инструкция запуска в README перестала бы быть полной.
- **marked, markdown-it и подобное** — одна команда `npm i -D`, но `CLAUDE.md`
  разрешает библиотеки только из списка в `docs/TOOLING.md`. Правило написано
  про продукт, и соблазн сказать «это же для документа, не считается» —
  ровно то, с чего список перестаёт работать. Если библиотека нужна,
  её добавляют в список вдвоём, а не в обход.
- **Ручная вёрстка отчёта в редакторе** — отчёт правится до последнего часа,
  и любая ручная сборка означала бы расхождение PDF с markdown.

**Цена решения.** Разбор markdown покрывает только то, что есть в наших
документах: заголовки, абзацы, списки, таблицы GFM, блоки кода, цитаты,
линии и строчную разметку. Вложенных списков и сносок он не умеет —
если они появятся, PDF молча выйдет кривым. Поэтому рядом с PDF кладётся
`*.debug.html` (в git не попадает): по нему видно, что именно ушло в печать.

---

## 28.08 · Шрифты лежат в репозитории, а не приезжают с Google

**Решение.** Четыре файла `woff2` в `public/fonts`, `@font-face`
в `src/index.css`, `<link>` из `index.html` снят. `unicode-range` сохранён
как у Google — браузер качает только нужные подмножества.

**Почему не оставили как было.** Не из-за приватности как таковой:
данные доски в Google и не уходили. Из-за обещания в первой строке README —
«бэкенда нет, данные живут в браузере». Приложение, которое без сети теряет
шрифты, это обещание нарушает. Нашлось при закрытии критерия приёмки 12,
который до 27 августа не проверялся ничем.

**Отвергнуто:**

- **`@fontsource/*` пакетами** — самый привычный путь, но это две новых
  зависимости, а `CLAUDE.md` разрешает только список из `docs/TOOLING.md`.
  Ради шрифтов список не расширяют.
- **Один файл на всю кириллицу и латиницу без `unicode-range`** — проще
  в объявлении и хуже в деле: браузер тянул бы латинские подмножества
  на русском интерфейсе, где они не нужны.
- **Оставить `<link>` и просто убрать обещание из README** — честно,
  но это лечение симптома: локальная доска, которой нужен интернет,
  остаётся странной.

**Цена решения.** 144 КБ в репозитории и ручное обновление, если понадобится
новая версия шрифта. Обновление редкое, конвейер не нужен; команда, которой
файлы получены, записана в `docs/TOOLS-INDEX.md` рядом с остальными следами
этого дня.

**Ловушка, в которую попали по дороге.** Golos Text — вариативный шрифт:
Google отдаёт для весов 400, 500 и 600 **один и тот же файл**, меняя только
`font-weight` в объявлении. Первая версия правки скачала их как три разных
и положила в репозиторий двенадцать файлов вместо четырёх. Поймано сверкой
контрольных сумм — `md5 -q public/fonts/*.woff2 | sort -u | wc -l` дало 9
при 17 файлах. Вывод общий: **скачал пачку файлов — сверь суммы**, одинаковые
имена не значат разное содержимое, и наоборот.

---

## 28.08 · Холст грузится отдельным чанком, вспомогательные функции — вне exportPng

**Решение.** Маршрут `/p/:projectId` обёрнут в `React.lazy` + `Suspense`;
`downloadUrl`, `safeFilename` и `ExportFailed` переехали из
`export/model/exportPng.ts` в `export/lib/download.ts`; `ProjectsHeader`
импортирует `ImportButton` напрямую, а не через баррель.

**Что это дало.** Первый экран 482 КБ вместо 927 (151 против 286 в gzip).
Предупреждение Vite про чанк больше 500 КБ снято.

**Почему одной ленивой загрузки не хватило.** Она выделила холст в свой чанк,
но Konva осталась во входном: экран списка тянул её через баррель
`features/export` (реэкспорт `exportPng`) и через `transfer.ts`, который брал
из `exportPng` три функции без единой строчки про Konva. Пока цепочка
не разорвана, ленивый маршрут экономит только собственный код экрана.

**Отвергнуто:**

- **`manualChunks` в конфиге сборки** — то, с чего задача начиналась.
  Выделило бы Konva в отдельный файл, но на первом экране он всё равно
  загружался бы: чанк отдельный, а импорт статический. Лечит вид отчёта
  о сборке, а не время загрузки.
- **Спиннер в `Suspense`** — холст приезжает за десятки миллисекунд,
  и спиннер на такой срок мигает и читается как поломка. Заглушка — пустой
  экран с тем же фоном.
- **Дробить дальше** (дизайн-система, стор, роутер отдельно) — выигрыш
  перестаёт быть заметным, а число запросов растёт.

**Цена решения.** Восемь e2e-сценариев упали: они клали фикстуру сразу после
смены адреса, а экран теперь монтируется позже и затирал её чтением
из IndexedDB. Ожидание появления холста добавлено в четыре helper'а —
в трёх других файлах оно уже стояло с комментарием, почему так надо.
Это и есть цена: предположение «экран монтируется синхронно» нигде
не было записано, просто было верным.


---

## 2026-08-28 · Находки агента-ломателя F-3 не чиним в день сдачи

**Решение.** Повторный прогон агента-ломателя по холсту (задача F-3) дал
11 находок — `docs/nightly/breaker/99-отчёт.md`, коммит `ac333f8`. Правки
под все 11, плюс открытые пункты прогона зоны B (`fitFontSize`, нормализация
`text` в `repair.ts`, `sweepBlobs`), собраны черновиком и **отложены в
`git stash`** до отдельного цикла с собственной проверкой.

**Почему не в день сдачи.** Две из находок про потерю данных, одна — про
обход инварианта; правки трогают `board.ts`, `operations.ts`, `repair.ts`,
`invariants.ts` — тот самый общий шов. Наспех починенный шов в последний час
перед сдачей — это ровно тот класс риска, за который в задании снимают больше,
чем за честно открытый дефект. Отчёт называет находки прямым текстом и
не выдаёт их за закрытые.

**Отвергнуто:**

- **Влить черновик как есть.** Тесты в нём зелёные (546/56), но прогон был
  один, без независимой проверки правок и без прогона ломателя по новому
  состоянию. Для правок в модели этого мало.
- **Убрать находки из отчёта и починить тихо.** Прямо противоречит критерию
  «честность» и всему тону отчёта.

**Цена решения.** Продукт уходит на сдачу с 11 известными дефектами, два из
которых — потеря данных обычным жестом. Записано в `README.md` и `REPORT.md`.

---

## 2026-08-28 · Слияние с `origin/main` перед сдачей, `npm run check` и CI отложены

**Решение.** Локальный `main` разъехался с `origin/main` на 1 и 6 коммитов:
напарник влил `setup/tooling` (прогон `pre-merge.md` по зоне B, тесты на
границу доверия, восстановленные сессии). Слито в `main` без конфликтов —
разделы отчёта не пересеклись. Единый скрипт `npm run check` и GitHub Actions
из того же черновика стабилизации отложены вместе с ним: обязательный прогон
остаётся ручным по `workflows/pre-merge.md`.

**Почему.** `check` и CI осмысленны поверх починенного кода, а не поверх кода
с 11 открытыми находками — иначе первый же прогон CI на `main` будет красным
или потребует ослабить пороги. Заводятся в том же цикле, что и починка.

---

## 2026-09-02 · Текст: цвет из темы, ресайз снимает `autoWidth`, четыре поля стиля

**Решение.** Три правки текстового узла одним заходом.

1. Цвет нового текста берётся из токена `--ink` (`canvas/nodes/textStyle.ts`,
   `themeInk`), а не из константы инструмента. На тёмной теме доска тёмная
   (`bg-paper`), и почти чёрный `#111827` был на ней невидим: узел создавался,
   но выглядел как «текст не создался».
2. `resizeNode` для текстового узла ставит `autoWidth: false`. Рендерер при
   `autoWidth` меряет строку в `useLayoutEffect` и возвращает рамку в модель —
   растянутый ручками блок схлопывался обратно, и ручки выглядели сломанными.
3. `TextStyle` получил `underline`, `letterSpacing`, `background`, `font`
   (контракт). Рисует их только `TextView` и `TextOverlay`; у стикера и подписей
   поля есть в модели, но пока не отрисованы — контролов на них тоже нет,
   чтобы в панели не было переключателей, которые ничего не делают.

**Почему цвет читается вычисленным, а не по `useTheme()`.** Тем три, и
«системная» решается медиазапросом; `getComputedStyle` даёт ответ для всех трёх
одной строкой. Кэша, в отличие от цвета сетки, нет намеренно: функция зовётся
на создание узла, а не на кадр.

**Почему гарнитура хранится ключом (`sans` | `serif` | `mono`), а не CSS-стеком.**
Файл открывается на чужой машине, где перечисленных шрифтов может не быть.
Стеки живут в зоне A (`canvas/nodes/textStyle.ts`), панель свойств знает только
ключи из контракта — стуба, как у палитры стикеров, заводить не пришлось.

**Отвергнуто:**

- **Ставить `autoWidth: false` в `SelectionTransformer`.** Зона A, без правки
  контракта — но мимо группового ресайза, который тоже зовёт `resizeNode`.
- **Красить текст по документу (`background.color`).** Доска этот цвет вообще
  не рисует — он живёт только в экспорте PNG. Отсюда известное следствие:
  светлый текст, набранный на тёмной теме, в экспорте ложится на светлый фон.
  Чинится либо отрисовкой фона документа на холсте, либо фоном экспорта из
  темы — обе правки шире этой задачи.

---

## 2026-09-02 · Буфер обмена — на событиях `copy`/`paste`, без своего буфера в памяти

**Решение.** `Cmd+C` / `Cmd+V` перехватываются слушателями событий `copy`
и `paste` (`features/shortcuts/model/useClipboard.ts`), а не строкой в таблице
tinykeys. В системный буфер кладётся текст: JSON с маркером `prostor-nodes`.
Вставка проверяет его той же zod-схемой, что и импорт файла (`nodeSchema`),
и зовёт новое действие общего шва `pasteNodes(nodes)`.

**Почему не свой буфер в памяти.** Он устаревает молча: скопировал объект,
потом скопировал текст в редакторе, вернулся, нажал `Cmd+V` — и получил объект.
Системный буфер — единственный источник правды о том, что человек копировал
последним. Заодно бесплатно получаются вставка между вкладками и проектами
и переживание перезагрузки.

**Почему не клавишами.** `Cmd+V` уже занят вставкой картинки (`useImageInsert`,
зона A) и работает через событие `paste`. Перехват нажатия клавиш съел бы
это событие, и картинки перестали бы вставляться. Теперь оба обработчика
делят случаи по содержимому буфера: файлы — картинке, наш JSON — узлам.

**Почему не `navigator.clipboard.readText()`.** В Firefox и Safari он просит
у пользователя разрешение. `event.clipboardData` доступен синхронно и без него.

**Набор копируемого — общий с `Cmd+D`** (`nodesToCopy` в `shared/model/operations`):
содержимое групп плюс линии, у которых скопированы оба конца. Логика была
внутри `duplicateNodes`, вынесена — иначе дублирование и копирование
разъехались бы, и объяснить разницу пользователю было бы нечем.

**Цена решения.** В буфере лежит JSON: вставка доски в текстовый редактор даст
текст, а не картинку. Узел картинки хранит `blobId`, а не сам файл, — вставка
в другом браузере или на другой машине даст узел без изображения. Обе цены
осознанные; вставка «как картинка» — это экспорт PNG, он уже есть.

---

## 2026-09-04 · Синхронизация через Supabase

**Решение.** Синхронизация досок между устройствами (задачи 1–7 плана
cloud-sync) построена на Supabase: Postgres-таблица `projects` для документов,
Supabase Auth для входа по почте и паролю, Supabase Storage для картинок.
Единственная новая зависимость — `@supabase/supabase-js`, запись в
[docs/TOOLING.md](TOOLING.md), раздел 1.3.

**Отвергнуто:**

- **Firebase.** С февраля 2026 Firebase Storage требует привязанную карту
  даже на бесплатном плане (Blaze) — для учебного проекта, который не должен
  просить платёжные данные, это стоп-фактор сам по себе, раньше самой оценки
  подходящей ли модели данных.
- **Своё API на Cloudflare Workers.** Аутентификацию, хранение паролей и
  выдачу токенов пришлось бы писать руками — это отдельный проект по объёму,
  а не строка в задаче. Supabase даёт Auth и RLS-политики готовыми, ценой
  чужой инфраструктуры вместо своей.

**Цена решения.** Заголовок README и `CLAUDE.md` держали обещание «бэкенда
нет, данные живут в браузере» с самого начала проекта. Оно перестало быть
безусловным: без аккаунта данные по-прежнему не покидают браузер (это и есть
суженный критерий приёмки 12 — `e2e/smoke.spec.ts` переименован и проверяет
ровно это, задача 8 плана cloud-sync), но с аккаунтом доска и картинки едут
на сторонний сервер, которым управляет не пользователь. Это честно назвать
ценой, а не улучшением без последствий: у синхронизации появляется
собственная поверхность отказа (сеть, лимиты бесплатного плана, чужой
Postgres) — она описана в README, разделы «Что умеет» и «Чего не умеет».

---

## 2026-09-04 · Оповещение между вкладками не сделано самодоставляющим

**Решение.** `publish`/`subscribe` в `features/persistence/sync.ts`
(`BroadcastChannel`) по-прежнему не доставляют событие той же вкладке, которая
его отправила — это стандартное поведение канала, и чинить его самодоставкой
не стали, хотя после задачи про выгрузку на сервер это выглядело очевидной
починкой: раз вкладка теперь узнаёт из `pushProject`, что документ уехал,
почему бы не сообщить об этом списку проектов тем же событием, которым уже
пользуются другие вкладки.

**Почему нет.** Проверено по коду, а не предположено: `overwriteProject`
(вызывается при скачивании чужой версии доски) шлёт `{ kind: 'saved',
projectId, updatedAt }`, и `useAutosave` в `features/persistence/autosave.ts`
трактует пришедший `saved` с чужим `updatedAt` как «доску переписала другая
вкладка» и показывает тост-конфликт, требующий обновить страницу. Если бы
`publish` доставлял событие самому себе, каждое собственное сохранение той же
вкладки било бы по этой же ветке и превращало обычную работу на открытой
доске в непрерывный ложный конфликт — ровно то, чего канал избегает по
умолчанию, но с чем предложенная починка столкнулась бы в лоб.

Вместо этого хук синхронизации (`useCloudSyncOnLogin`) после круга слияния
дёргает `bumpRevision()` из `features/projects/dialogsStore.ts` — тот же
приём, которым уже пользуются диалоги списка проектов (`ProjectDialogs.tsx`)
для «список изменился, перерисуйся». Список проектов и так живёт в той же
вкладке, где произошло слияние, — событие между вкладками ему не нужно вовсе.

**Цена решения.** `features/cloud` (зона B) импортирует `bumpRevision` из
`features/projects` (тоже зона B) — межслайсовый импорт внутри одной зоны,
не через `shared/`. Пока это один вызов в одном месте, заводить для него
отдельный маленький стор избыточно. Если счётчик понадобится ещё где-то —
выносится в `shared/`, тогда и меняется.
