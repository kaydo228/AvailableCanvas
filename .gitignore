node_modules
dist/
.DS_Store
*.local
.env
.env.*
!.env.example
.claude/settings.local.json
.claude/.stop-gate-tripped
playwright-report/
test-results/

# docs/PROMPTS-*.md был здесь до 27 августа. Убран из списка: docs/SCORING.md
# ссылается на эти промпты как на доказательство («промпт A-12», «S-4»),
# а проверяющий по такой ссылке не пройдёт, если файла нет в репозитории.
# Секретов в них нет — проверено перед добавлением.

# Имя разработчика для журнала сессий — у каждого своё.
.claude/dev-name

# Промежуточный HTML сборки PDF — рядом с самим PDF он не нужен.
*.debug.html

# Индекс кодовой базы (эксперимент F-1): артефакты и локальный скилл.
graphify-out/
.claude/skills/graphify/
